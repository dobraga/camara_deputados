CREATE TABLE IF NOT EXISTS tbl_partidos (
    id text not null,
    url_base text,
    nm_partido text,
    nm_sigla text,
    id_lider text,
    st_partido text,
    qt_posse text,
    qt_membros text,
    id_legislatura text,
    dt_evento text
);